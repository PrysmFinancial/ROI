# Local PostgreSQL uses your macOS username via a unix socket (no password required).
# Databases: roi_development, roi_test
#
# Optional explicit connection URL:
# DATABASE_URL=postgres://localhost/roi_development
